// 11단계 팔레트 생성을 제어하는 현행 호환 매개변수입니다.
// 초기 설계는 Tailwind·Radix·Leonardo를 참고했다고 설명했으나 아래 네 표의
// 정확한 원표·변환식·최초 취득 경로는 확인되지 않았습니다.
// 2026-09-03 고정 공식 자료와 사용처를 대조했습니다. 공식 수치의 직접 이식이나
// 전부 독자 창작이라고 단정하지 않으며 기존 결과 보존을 위해 값은 변경하지 않습니다.
// 공개 후보의 docs/COLOR-PROVENANCE.md에 대조 범위와 미확인 사항을 기록합니다.

export const STEPS = [50, 100, 200, 300, 400, 500, 600, 700, 800, 900, 950] as const;
export type Step = (typeof STEPS)[number];

/** 단계별 OKLCH 목표 명도(0..1)입니다. 특정 공식 색상군의 그대로 이식된 표라고 주장하지 않습니다. */
export const L_TABLE: Record<Step, number> = {
  50: 0.975, 100: 0.945, 200: 0.895, 300: 0.83, 400: 0.72, 500: 0.63,
  600: 0.55, 700: 0.47, 800: 0.39, 900: 0.32, 950: 0.23,
};

/** 기준 채도에 곱하는 단계별 감쇠 계수입니다. 색역 제한과 함께 사용하는 제품 매개변수입니다. */
export const CHROMA_MUL: Record<Step, number> = {
  50: 0.18, 100: 0.3, 200: 0.55, 300: 0.78, 400: 0.94, 500: 1.0,
  600: 0.95, 700: 0.85, 800: 0.7, 900: 0.55, 950: 0.42,
};

/** 프리셋이 요청할 때만 사용하는 단계별 색상각 이동값(도)입니다. */
export const HUE_SHIFT: Record<Step, number> = {
  50: 4, 100: 3, 200: 2, 300: 1, 400: 0, 500: -1,
  600: -2, 700: -3.5, 800: -5, 900: -6.5, 950: -8,
};

/** 기준 배경에 대한 목표 대비 매개변수입니다. Leonardo가 정한 공식 고정 비율표는 아닙니다. */
export const LEONARDO_RATIOS: Record<Step, number> = {
  50: 1.05, 100: 1.15, 200: 1.3, 300: 1.8, 400: 2.6, 500: 3.5,
  600: 4.5, 700: 6.0, 800: 8.5, 900: 12, 950: 15,
};
